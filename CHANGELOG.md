# 更新日志 (CHANGELOG)

本项目基于 [gulihua10010/mcp-server-article](https://github.com/gulihua10010/mcp-server-article) 二次开发。
所有变更均通过浏览器抓包（对照官方创作中心编辑器的真实请求）验证后实现，接口行为确认依据见文末。

## [1.1.0] - 2026-09-14（CSDN 发文增强版）

### 针对发布工具 `publishArticle2Csdn` 的新增参数

| 参数 | 类型 | 说明 |
|---|---|---|
| `id` | Long | 传入则**更新已有文章/草稿**，不传则新建（对齐官方编辑器行为：更新时请求体携带 id） |
| `tags` | String | 自定义标签，英文逗号分隔（如 `人工智能,Java`）；不传默认 `后端`（原版本写死） |
| `categories` | String | 分类专栏，英文逗号分隔，需与已有专栏名完全一致（如 `Java基础`）。**注意：CSDN 平台行为——仅在发布（status=0）时持久化，存草稿时服务端忽略该字段**（官方编辑器同样如此） |
| `draft` | Boolean | `true`=仅存草稿箱；`false`=直接发布。**存草稿修复**：原版本无此功能，且错误使用 `status=1`（无效），正确参数为 `status=2` + `pubStatus=draft`（抓包自官方编辑器） |
| `dryRun` | Boolean | `true`=只返回将要发送的完整请求体，不调用接口。用于调试新参数，零风险 |

### Bug 修复

- **摘要空指针**：原代码 `StrUtil.isBlank(desc) && desc.length() < 50` 短路顺序错误，`description` 为 null 时必现 NPE；
- **存草稿响应解析崩溃**：CSDN 存草稿接口响应的 `data` 是含 id/url 的对象（与发布一致），按实际结构修正解析；此前按"data 为字符串"的实现会导致解析异常；
- **接口超时过短**：3s → 30s（长文发布经常超 3 秒）。

### 参数规格对齐

- 摘要长度上限 100 → **256 字**（与创作中心编辑器限制一致）。

### 验证记录（2026-09-13/14，均为实测）

- ✅ 发布（status=0）：测试文章成功发布，标签/分类专栏/摘要均正确持久化（文章页"收录于"可见）；
- ✅ 存草稿（status=2 + pubStatus=draft）：草稿成功出现在创作中心草稿箱，可编辑/预览/发布；
- ✅ 更新（带 id）：正确更新目标草稿，不新建；
- ✅ dryRun：返回完整请求体，无网络请求；
- ⚠️ 已知平台限制：草稿阶段 `categories` 不持久化（含官方编辑器），发布时才生效。

### 安全/合规说明

- Cookie 仅通过环境变量 `CSDN_COOKIE` 本地注入，不落盘、不外发；
- 建议单日自动发文 ≤ 2 篇，内容人工审核后再发（CSDN 对异常频率有风控）。

## [1.0.0] - 2025-04（上游版本）

- 上游功能：发布到 CSDN/掘金/博客园、牛客面经抓取、科技资讯抓取。

---

### 接口行为确认方法

所有 CSDN 接口变更均先在官方创作中心编辑器操作并通过注入 `XMLHttpRequest` Proxy 钩子抓包，
逐字段对比请求体/响应体后实现（官方编辑器存草稿请求样本：`status=2`、`pubStatus=draft`、
`Description` 大写 D、更新场景带 `id`）。**接口返回 code=200 不代表业务生效，必须以页面实际状态二次确认。**
